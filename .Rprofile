source("renv/activate.R")

# For reproducibility, freeze package versions

# Mac OS
if(Sys.info()['sysname'][[1]]=="Darwin") options(repos = c(REPO_NAME = "https://packagemanager.posit.co/cran/25-04-2024"))

# Linux Ubuntu
if(Sys.info()['sysname'][[1]]=="Linux")options(repos = c(REPO_NAME = "https://packagemanager.posit.co/cran/__linux__/noble/25-04-2024"))
